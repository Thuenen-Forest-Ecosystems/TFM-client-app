org.gradle.jvmargs=-Xmx4G -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true

#https://stackoverflow.com/questions/77520506/inconsistent-jvm-target-compatibility-detected-for-tasks-despite-already-definin
kotlin.jvm.target.validation.mode = IGNORE

# Set Java version for Gradle
# org.gradle.java.home is commented out to allow automatic detection
# Uncomment and set the path for local development if needed
# org.gradle.java.home=/Library/Java/JavaVirtualMachines/temurin-21.jdk/Contents/Home

# Windows-specific SSL settings (only for local Windows development)
# Uncomment these lines on Windows if you have corporate network certificate issues
# systemProp.javax.net.ssl.trustStoreType=WINDOWS-ROOT
# systemProp.javax.net.ssl.trustStore=NUL
# systemProp.javax.net.ssl.trustStorePassword=changeit