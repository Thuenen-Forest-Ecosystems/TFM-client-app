DMZ_KEY=
# Base64 X25519 public key of whoever may open a support database backup.
# Unset => backups are written as plain ZIPs. Generate with:
#   node tools/tfm-backup.mjs keygen
# The matching private key is NOT part of this repo — escrow it, losing it
# makes every backup unreadable.
BACKUP_RECIPIENT_KEY=
